/**
 * Feature mount point: Changes (Wave 2 / feature module).
 * W1-A shell shows Changes tab empty placeholder only.
 */
export const CHANGES_FEATURE_MOUNT = "apps/desktop/src/features/changes" as const;
