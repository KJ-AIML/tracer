/**
 * Feature mount point: Terminal events UI (optional capability).
 * W1-A does not invent a PTY product surface.
 */
export const TERMINAL_FEATURE_MOUNT = "apps/desktop/src/features/terminal" as const;
