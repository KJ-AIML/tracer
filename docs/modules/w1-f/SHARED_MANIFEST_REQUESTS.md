# W1-F Shared Manifest Requests

## Applied in this worktree (minimal root registration)

| File | Change |
|---|---|
| `Cargo.toml` workspace members | Added `crates/tracer-control-plane` |
| `Cargo.toml` workspace.dependencies | Added `tracer-control-plane = { path = ... }` |
| `apps/desktop/src-tauri/Cargo.toml` | Deps: `tracer-control-plane`, `tokio`, `tracer-domain` |
| `Cargo.lock` | Regenerated by cargo |

## Deferred / none

No foundation crate redesigns. No `repos/grok-build` edits.
