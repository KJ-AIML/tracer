# Release fixtures (metadata only)

This directory holds **schema and documentation** for upgrade/provenance fixtures.

- Binaries, installers, SQLite DBs, and credentials are **never** committed.
- Built artifacts stage under `target/release-rc/upgrade-fixture/` (gitignored).
- See `provenance.schema.json` and `docs/modules/w2-4-1/`.