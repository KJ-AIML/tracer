{
  "name": "@tracer/tauri-driver-tools",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "W2.2-T helpers: tauri-driver + msedgedriver plan/apply provisioning (no auto-install without opt-in)",
  "scripts": {
    "doctor": "node ./doctor.mjs",
    "start": "node ./start-driver.mjs",
    "print-setup": "node ./print-setup.mjs",
    "setup": "node ./setup.mjs",
    "setup:apply": "node ./setup.mjs --apply"
  },
  "engines": {
    "node": ">=20"
  }
}